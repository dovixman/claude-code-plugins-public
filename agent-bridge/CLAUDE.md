# Agent Bridge Plugin

## Versioning

The plugin version must be kept in sync across both files:

- `package.json` (`version` field)
- `.claude-plugin/plugin.json` (`version` field)

When bumping the version, always update both files to the same value.
